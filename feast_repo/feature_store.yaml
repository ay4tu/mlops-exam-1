project: modelserve
provider: local
registry:
  registry_type: file
  path: data/registry.db
online_store:
  type: redis
  # overridden at runtime via REDIS_HOST / REDIS_PORT env vars in feature_client.py
  connection_string: "localhost:6379"
offline_store:
  type: file
entity_key_serialization_version: 2
